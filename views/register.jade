extends layout

block content
 h2.page-header Register
 p Please register using the form below
 if errors
  each error, i in errors
   div.alert.alert-danger #{error.msg}
 form(method='post', action='/users/register')
  .form-group
   label Login
   input.form-control(name='login', type='text', placeholder='Enter your Login')
  .form-group
   label Email
   input.form-control(name='email', type='email', placeholder='Enter Email')
  .form-group
   label Password
   input.form-control(name='password', type='password', placeholder='Enter Password')
  .form-group
   label First name
   input.form-control(name='firstName', type='text', placeholder='Enter first name')

  .form-group
   label Last name
   input.form-control(name='lastName', type='text', placeholder='Enter last name')

  .form-group
   label Phone number
   input.form-control(name='phoneNumber', type='text', placeholder='Enter phone number')

  input.btn.btn-primary(type='submit', name='submit', value='Register')
